spring:
    data:
      mongodb.host: localhost
      mongodb.port: 27017
      mongodb.name: cms-test

logging:
    level.org.springframework: INFO
    level.ms.cms: DEBUG

server:
    port: 9000

management:
    port: 9001
